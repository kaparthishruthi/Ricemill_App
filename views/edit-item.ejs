<!DOCTYPE html>
<html>
<head>
  <title>Edit Item</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f6f8;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .container {
      background: white;
      padding: 30px 40px;
      border-radius: 8px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      width: 400px;
    }
    h1 {
      margin-bottom: 20px;
      color: #333;
      text-align: center;
    }
    input[type="text"],
    input[type="number"] {
      width: 100%;
      padding: 12px;
      margin: 10px 0 20px 0;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 16px;
    }
    button {
      width: 100%;
      padding: 12px;
      background-color: #2874f0;
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 18px;
      cursor: pointer;
    }
    button:hover {
      background-color: #165ec9;
    }
    .top-nav {
      margin-bottom: 15px;
      text-align: center;
    }
    .top-nav a {
      color: #2874f0;
      text-decoration: none;
      font-weight: bold;
    }
    .top-nav a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="top-nav">
      <a href="/items">Back to Items</a>
    </div>
    <h1>Edit Item</h1>
    <form action="/items/edit/<%= item._id %>" method="POST">
      <input type="text" name="itemName" value="<%= item.itemName %>" placeholder="Item Name" required />
      <input type="number" name="quantity" value="<%= item.quantity %>" placeholder="Quantity" min="0" required />
      <input type="number" step="0.01" name="pricePerUnit" value="<%= item.pricePerUnit %>" placeholder="Price per Unit" min="0" required />
      <button type="submit">Update Item</button>
    </form>
  </div>
</body>
</html>
